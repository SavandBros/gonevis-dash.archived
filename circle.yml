machine:
  node:
    version: 8.2.0
dependencies:
  override:
    - npm install -g codecov
    - npm install
test:
  post:
    - codecov
deployment:
  production:
    branch: production
    commands:
      - ./scripts/deploy.sh
