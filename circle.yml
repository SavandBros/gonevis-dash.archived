dependencies:
  override:
    - npm install
test:
  override:
    - 'npm test'
  post:
    - bash <(curl -s https://codecov.io/bash)
deployment:
  production:
    branch: production
    commands:
      - ./scripts/deploy.sh
